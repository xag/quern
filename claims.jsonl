{"kind": "done", "text": "a project declares its ledger entry once in pyproject.toml ([tool.quern] ledger = \"PATH[:ATTR]\") and brief/navigate/check find it without --module; the override still wins", "session": "e0f996ae-8e63-4f40-9f6e-940844d97431", "evidence": [{"where": "producer", "what": "204 tests; test_a_project_may_declare_its_ledger_in_pyproject covers declared-with-attr, declared-without, and --module over both"}, {"where": "stand-in", "what": "the case that motivated it - a library whose ledger is epure/tree.py - reproduced in the test's layout", "gap": "that library's own pyproject line is the other session's to add; until then its brief still needs --module"}]}
{"kind": "done", "session": "79513efe-44ea-44ce-8d7b-d30582be4bd7", "text": "`quern estate DIR`: the fleet's owings computed from the ledgers rather than kept beside them. 15 projects with a ledger, 15 read, 36 debts open and 21 entries red. Each ledger is read in its OWN environment - the module form before any console script, and the caller's VIRTUAL_ENV dropped, because `uv run quern` finds an executable on PATH when the project has none and a project that cannot answer then appears to answer through the caller's quern. A ledger that does not answer is printed under its own heading with a non-zero exit, never omitted", "decided_at": "ledger: the-fleets-owings-are-computed-never-authored (two alternatives rejected, including the cross-project bucket ledger)", "evidence": [{"where": "producer", "what": "quern estate over the estate: 15 of 15 read, 14 via `python -m quern.cli brief` in the project's own environment and 1 via the no-project fallback (the package quern depends on, which cannot depend back); 207 tests green including three new ones - a discharged debt is not counted as owed, an unread project is reported not skipped, a ledger is found where it lives or where pyproject says"}, {"where": "stand-in", "what": "the PATH leak was caught by running the same project's brief on its own and getting `program not found` where the roll-up had reported success", "gap": "run from this machine and this shell only; a CI environment with a different PATH has not been tried"}]}
{"kind": "done", "session": "b140da27-6325-4548-b0fe-091ed49dca92", "text": "estate --json carries each owed entry's links as structured data, parsed beside the renderer and bound to it by a round-trip test", "evidence": [{"where": "user-surface", "what": "the consumer's stated gap was the reason for this change (quality-harness derived edges by regexing brief lines, where a rendering change would drop every edge silently); the live estate run now emits links for twenty owed entries across the fleet, including all four blocked_by declarations that consumer derives its ordering from", "gap": "the consumer itself still parses lines until it repins quern and reads the links field - the next commit in its repo"}, {"where": "producer", "what": "the round-trip test renders a line with brief._line and harvests it back to the same links dict, so renderer and parser break together in this repo; a prose arrow inside a bit is shown not to parse as a link; 210 tests green"}]}
